services:
  - type: web
    name: epidemic-toolkit
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn epidemic_toolkit.api.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.6
